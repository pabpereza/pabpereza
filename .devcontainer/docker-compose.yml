services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      # Mount the workspace
      - ..:/workspace:cached
      # Mount SSH agent for Git operations (if available)
      - ~/.ssh:/home/node/.ssh:ro
      # Persist shell history
      - devcontainer-bash-history:/home/node/.bash_history
      # Persist ZSH history
      - devcontainer-zsh-history:/home/node/.zsh_history
      # Persist vim configuration
      - devcontainer-vim-config:/home/node/.vim
      # Persist neovim configuration
      - devcontainer-nvim-config:/home/node/.config/nvim
    ports:
      # Docusaurus development server
      - "3000:3000"
      # Node.js debug ports
      - "9229:9229"
      - "9230:9230"
    environment:
      - NODE_ENV=development
      - EDITOR=vim
    working_dir: /workspace
    command: sleep infinity
    # Enable init process for proper signal handling
    init: true

volumes:
  devcontainer-bash-history:
  devcontainer-zsh-history:
  devcontainer-vim-config:
  devcontainer-nvim-config: