---
title: Idempotencia avanzada y módulos de producción en Ansible
description: >-
  Domina la idempotencia real con --check, --diff, changed_when y failed_when.
  Aprende template con Jinja2, lineinfile, command/shell seguros y módulos de
  aprovisionamiento para despliegues de producción.
keywords:
  - ansible idempotencia avanzada
  - ansible template jinja2
  - ansible lineinfile
  - ansible changed_when
  - ansible check diff
  - ansible command shell idempotente
  - ansible get_url git
  - módulos ansible producción
  - ansible ansible.builtin
  - idempotencia ansible qué es
  - ansible --check mode
  - ansible módulos más usados
sidebar_label: 4. Idempotencia y módulos avanzados
sidebar_position: 4
tags:
  - ansible
  - modulos
  - idempotencia
  - jinja2
image: 'https://pabpereza.dev/img/banner_ansible.png'
slug: ansible_modulos_idempotencia
---


# Idempotencia avanzada y módulos de producción 🔬

En el módulo anterior aprendiste los módulos esenciales y el concepto de idempotencia. Ahora toca profundizar: ¿qué pasa cuando los módulos *no* son idempotentes? ¿Cómo generamos configuraciones dinámicas? ¿Cómo cirugía un archivo de config sin reemplazarlo entero?

:::info Video pendiente de grabación
:::

## Idempotencia en la práctica: `--check` y `--diff`

Antes de tocar nada en producción, necesitas responder a la pregunta: **¿qué va a cambiar exactamente?**

```bash
ansible-playbook site.yml --check --diff
```

- `--check`: modo *dry-run*, no modifica nada en el sistema.
- `--diff`: muestra los cambios línea a línea, como un `git diff`.

Cuando combinas ambos, Ansible te dice exactamente qué modificaría sin tocar nada. Esto es tu red de seguridad antes de cada deploy.

### La regla de oro de la idempotencia

> Un playbook bien escrito debe terminar con `changed=0` cuando lo ejecutas **dos veces seguidas**. Si el segundo run reporta cambios, tienes un problema de idempotencia.

```bash
# Primera ejecución: configura el sistema
ansible-playbook site.yml
# → changed=5, failed=0

# Segunda ejecución sin tocar nada: todo debe estar ok
ansible-playbook site.yml
# → changed=0, failed=0  
```

Si la segunda ejecución sigue reportando `changed`, hay una task que no respeta el estado deseado.


## `ansible-doc`: tu documentación offline

No necesitas internet para consultar qué hace cada módulo. Ansible incluye documentación completa en local:

```bash
# Buscar módulos relacionados con "template"
ansible-doc -l | grep template

# Ver documentación completa
ansible-doc ansible.builtin.template

# Ver solo los parámetros (resumen rápido)
ansible-doc -s ansible.builtin.template

# Ver los ejemplos de uso
ansible-doc ansible.builtin.template | grep -A 30 EXAMPLES
```

Úsalo antes de buscar en Google. Suele ser más preciso y funciona sin conexión.


## `template`: de archivos estáticos a configuraciones dinámicas

En el módulo anterior usamos `copy` para subir archivos. `copy` es estático: lo que sube es exactamente lo que hay en tu PC. `template` es su hermano mayor: procesa el archivo con **Jinja2** antes de subirlo, permitiendo usar variables, condicionales y loops dentro del propio archivo de configuración.

### La analogía: la plantilla de Word

Imagina una plantilla de carta formal donde cambias el nombre del destinatario, la fecha y el motivo. El cuerpo es siempre el mismo, pero los datos son dinámicos. Eso es exactamente lo que hace `template` con tus configs.

### Sintaxis básica

```yaml
- name: Generar configuración de Nginx
  template:
    src: templates/nginx.conf.j2    # Plantilla local (extensión .j2 por convención)
    dest: /etc/nginx/nginx.conf
    owner: root
    group: root
    mode: '0644'
  notify: Recargar nginx
```

### Sintaxis Jinja2 en templates

Hay tres constructores que usarás el 90% del tiempo:

```jinja2
{# Esto es un comentario Jinja2 — no aparece en el archivo final #}

{# 1. Variables: doble llave #}
server_name {{ domain_name }};
listen {{ http_port | default(80) }};

{# 2. Condicionales: bloques if #}
{% if enable_ssl %}
listen 443 ssl;
ssl_certificate /etc/ssl/{{ domain_name }}.crt;
{% endif %}

{# 3. Loops: iterar sobre listas #}
upstream backend {
{% for server in backend_servers %}
    server {{ server }}:8080;
{% endfor %}
}
```

### Ejemplo completo: nginx.conf.j2

```jinja2
# Generado por Ansible — NO editar manualmente
# Host: {{ ansible_hostname }} | Fecha: {{ ansible_date_time.date }}

user nginx;
worker_processes {{ ansible_processor_vcpus | default(2) }};

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    server {
        listen {{ http_port | default(80) }};
        server_name {{ server_name }};
        root {{ document_root }};
        index index.html index.php;

{% if enable_gzip | default(false) %}
        gzip on;
        gzip_types text/plain application/json;
{% endif %}

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
```

Y el playbook que lo usa:

```yaml
- name: Configurar Nginx con template
  hosts: webservers
  become: yes

  vars:
    http_port: 80
    server_name: "miapp.ejemplo.com"
    document_root: /var/www/miapp
    enable_gzip: true

  tasks:
    - name: Desplegar configuración de Nginx
      template:
        src: templates/nginx.conf.j2
        dest: /etc/nginx/nginx.conf
        mode: '0644'
        validate: 'nginx -t -c %s'  # Valida antes de aplicar
      notify: Recargar nginx

  handlers:
    - name: Recargar nginx
      service:
        name: nginx
        state: reloaded
```

> El parámetro `validate` es clave en producción: si la configuración generada tiene errores de sintaxis, Ansible la rechaza antes de sobrescribir la buena. Nginx nunca ve una config rota.


## `lineinfile` y `blockinfile`: cirugía de archivos

A veces no quieres reemplazar un archivo entero, solo modificar una línea específica o añadir un bloque. Para eso están estos dos módulos.

### `lineinfile`: gestionar una línea

Asegura que una línea concreta existe (o no) en un archivo. Usa `regexp` para encontrar la línea existente y `line` para reemplazarla.

```yaml
# Deshabilitar login de root por SSH
- name: Configurar SSH - deshabilitar root login
  lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '^PermitRootLogin'   # Regex para encontrar la línea (aunque esté comentada)
    line: 'PermitRootLogin no'    # Valor final deseado
    state: present
  notify: Reiniciar SSH

# Ajustar límite de descriptores de archivo
- name: Configurar límite de archivos abiertos
  lineinfile:
    path: /etc/security/limits.conf
    line: '* soft nofile 65536'
    insertafter: EOF             # Si no existe, añadir al final
```

### `blockinfile`: gestionar un bloque de líneas

Cuando necesitas insertar o actualizar varias líneas a la vez. Ansible añade marcadores automáticos para saber dónde está el bloque y poder actualizarlo de forma idempotente.

```yaml
- name: Añadir configuración de swap a sysctl
  blockinfile:
    path: /etc/sysctl.conf
    marker: "# {mark} ANSIBLE MANAGED BLOCK - swap"
    block: |
      vm.swappiness=10
      vm.vfs_cache_pressure=50
  notify: Aplicar sysctl
```

El bloque resultante en el archivo queda así:

```
# BEGIN ANSIBLE MANAGED BLOCK - swap
vm.swappiness=10
vm.vfs_cache_pressure=50
# END ANSIBLE MANAGED BLOCK - swap
```

Si vuelves a ejecutar el playbook, Ansible encuentra los marcadores, compara el contenido y solo actúa si el bloque es diferente. Idempotente.


## `command` y `shell`: domar lo no idempotente

Los módulos `command` y `shell` ejecutan comandos arbitrarios y **no son idempotentes por defecto**. Ansible no sabe si la segunda ejecución del comando produce cambios o no. Por eso hay que enseñárselo.

### El problema

```yaml
# ❌ Esto se ejecuta SIEMPRE y siempre reporta changed
- name: Compilar aplicación
  command: make install
  args:
    chdir: /opt/miapp
```

### Solución 1: `creates` / `removes`

Le dices a Ansible: "ejecuta este comando solo si este archivo NO existe" (`creates`) o "solo si SÍ existe" (`removes`).

```yaml
# ✅ Solo compila si el binario no existe todavía
- name: Compilar aplicación
  command: make install
  args:
    chdir: /opt/miapp
    creates: /usr/local/bin/miapp  # Skip si este archivo ya existe
```

### Solución 2: `register` + `changed_when`

Captura la salida del comando y define tú mismo cuándo se considera un cambio.

```yaml
# Obtener estado actual
- name: Verificar si la base de datos está inicializada
  command: mysql -e "SHOW DATABASES LIKE 'app';"
  register: db_check
  changed_when: false       # Este comando NUNCA provoca changed (es solo una consulta)
  failed_when: db_check.rc != 0 and 'Access denied' not in db_check.stderr

# Actuar según el resultado
- name: Inicializar base de datos si no existe
  command: /opt/app/bin/init-db.sh
  when: "'app' not in db_check.stdout"
```

### Solución 3: `changed_when` con condición en la salida

Algunos comandos te dicen si hicieron cambios en su output:

```yaml
- name: Aplicar migraciones de base de datos
  command: python manage.py migrate
  register: migrate_result
  changed_when: "'No migrations to apply' not in migrate_result.stdout"
```

### Tabla resumen

| Situación | Solución |
|-----------|----------|
| El comando crea un archivo predecible | `creates: /ruta/al/archivo` |
| El comando es una consulta/lectura | `changed_when: false` |
| El output indica si hubo cambio | `changed_when: 'texto' not in result.stdout` |
| Necesitas resultado para decidir | `register:` + `when:` en task siguiente |


## Módulos de aprovisionamiento

### `get_url`: descargar archivos

```yaml
- name: Descargar binario de Prometheus
  get_url:
    url: "https://github.com/prometheus/prometheus/releases/download/v2.45.0/prometheus-2.45.0.linux-amd64.tar.gz"
    dest: /tmp/prometheus.tar.gz
    checksum: "sha256:1c7f489a3cc919c1ed0df2ae673a280309dc4a3eaa6ee3411e7d1f4bdec4d4c5"
    mode: '0644'
```

El parámetro `checksum` verifica la integridad del archivo. Si el hash no coincide, la task falla. Si el archivo ya existe con el hash correcto, Ansible no lo descarga de nuevo. Idempotente.

### `unarchive`: descomprimir archivos

```yaml
# Descomprimir un archivo local
- name: Descomprimir Prometheus
  unarchive:
    src: /tmp/prometheus.tar.gz
    dest: /opt/
    remote_src: yes           # El archivo está en el servidor remoto, no en tu PC
    creates: /opt/prometheus  # Skip si el directorio ya existe
```

### `git`: clonar y actualizar repositorios

```yaml
- name: Clonar repositorio de la aplicación
  git:
    repo: https://github.com/miorg/miapp.git
    dest: /opt/miapp
    version: "v2.1.0"         # Branch, tag o commit hash (evita 'main' en producción)
    update: yes               # Pull si ya existe el repo
    force: no                 # No sobreescribir cambios locales
```

> En producción, apunta siempre a un tag o commit hash, nunca a `main`. Así sabes exactamente qué versión estás desplegando.

### `authorized_key`: gestionar acceso SSH

```yaml
- name: Añadir clave SSH del equipo de DevOps
  authorized_key:
    user: deployer
    state: present
    key: "{{ lookup('file', 'files/devops_team.pub') }}"
    # También acepta una URL pública de GitHub:
    # key: https://github.com/usuario.keys
```

Cada ejecución verifica que la clave está en `~/.ssh/authorized_keys`. Si ya está, no toca nada. Si la eliminas manualmente del servidor, el próximo run la restaura. Convergencia real.


## Práctica: despliegue WordPress multi-host

Integramos todos los conceptos del módulo en un despliegue de dos capas: base de datos en `target3` y servidor web en `target2`. Usamos templates, `lineinfile`, `get_url`, `changed_when` y el patrón de idempotencia completo.

### Play 1 — Base de datos en `target3`

```yaml
- name: Configurar MariaDB en target3
  hosts: target3
  become: yes

  vars:
    db_name: wordpress
    db_user: wordpress
    db_password: wordpress_pass

  tasks:
    - name: Instalar MariaDB y dependencias Python
      apt:
        name:
          - mariadb-server
          - python3-pymysql    # Necesario para que Ansible gestione MySQL
        state: present
        update_cache: yes

    - name: Arrancar y habilitar MariaDB
      service:
        name: mariadb
        state: started
        enabled: yes

    - name: Crear base de datos WordPress
      community.mysql.mysql_db:
        name: "{{ db_name }}"
        state: present
        login_unix_socket: /var/run/mysqld/mysqld.sock

    - name: Crear usuario con acceso remoto
      community.mysql.mysql_user:
        name: "{{ db_user }}"
        password: "{{ db_password }}"
        priv: "{{ db_name }}.*:ALL"
        host: "%"             # Cualquier host puede conectar
        state: present
        login_unix_socket: /var/run/mysqld/mysqld.sock

    - name: Permitir conexiones remotas en MariaDB
      lineinfile:
        path: /etc/mysql/mariadb.conf.d/50-server.cnf
        regexp: "^bind-address"
        line: "bind-address = 0.0.0.0"
      notify: Reiniciar MariaDB

  handlers:
    - name: Reiniciar MariaDB
      service:
        name: mariadb
        state: restarted
```

### Play 2 — Servidor web + WordPress en `target2`

```yaml
- name: Configurar Nginx + WordPress en target2
  hosts: target2
  become: yes

  vars:
    db_host: target3
    db_name: wordpress
    db_user: wordpress
    db_password: wordpress_pass
    wp_dir: /var/www/wordpress
    wp_version: "6.5.5"

  tasks:
    - name: Instalar nginx, PHP-FPM y extensiones
      apt:
        name:
          - nginx
          - php-fpm
          - php-mysql
          - php-curl
          - php-gd
          - php-mbstring
          - php-xml
          - php-zip
          - unzip
          - curl
        state: present
        update_cache: yes

    - name: Obtener versión de PHP instalada
      command: php -r 'echo PHP_MAJOR_VERSION.".".PHP_MINOR_VERSION;'
      register: php_ver
      changed_when: false       # Solo consulta, nunca provoca changed

    - name: Registrar versión de PHP como fact
      set_fact:
        php_version: "{{ php_ver.stdout }}"

    - name: Crear usuario del sistema para WordPress
      user:
        name: wordpress
        shell: /usr/sbin/nologin
        system: yes

    - name: Crear directorio web
      file:
        path: "{{ wp_dir }}"
        state: directory
        owner: wordpress
        group: www-data
        mode: '0755'

    - name: Descargar WordPress
      get_url:
        url: "https://wordpress.org/wordpress-{{ wp_version }}.tar.gz"
        dest: /tmp/wordpress.tar.gz
        checksum: "sha1:4b2d3a99f96d6df0b9c1e4a3e7f8b6c2d1a0e5f3"  # Verificar integridad

    - name: Descomprimir WordPress
      unarchive:
        src: /tmp/wordpress.tar.gz
        dest: /tmp/
        remote_src: yes
        creates: /tmp/wordpress   # Idempotente: skip si ya existe

    - name: Copiar WordPress al directorio web
      copy:
        src: /tmp/wordpress/
        dest: "{{ wp_dir }}/"
        owner: wordpress
        group: www-data
        remote_src: yes

    - name: Generar wp-config.php con template
      template:
        src: templates/wp-config.php.j2
        dest: "{{ wp_dir }}/wp-config.php"
        owner: wordpress
        group: www-data
        mode: '0640'

    - name: Configurar Nginx para WordPress
      template:
        src: templates/nginx-wordpress.conf.j2
        dest: /etc/nginx/sites-available/wordpress
        mode: '0644'
        validate: 'nginx -t -c /dev/stdin'
      notify: Recargar nginx

    - name: Activar sitio WordPress
      file:
        src: /etc/nginx/sites-available/wordpress
        dest: /etc/nginx/sites-enabled/wordpress
        state: link
      notify: Recargar nginx

    - name: Desactivar sitio default
      file:
        path: /etc/nginx/sites-enabled/default
        state: absent
      notify: Recargar nginx

    - name: Arrancar nginx y PHP-FPM
      service:
        name: "{{ item }}"
        state: started
        enabled: yes
      loop:
        - nginx
        - "php{{ php_version }}-fpm"

  handlers:
    - name: Recargar nginx
      service:
        name: nginx
        state: reloaded
```

### Template `templates/wp-config.php.j2`

```jinja2
<?php
define('DB_NAME',     '{{ db_name }}');
define('DB_USER',     '{{ db_user }}');
define('DB_PASSWORD', '{{ db_password }}');
define('DB_HOST',     '{{ db_host }}');
define('DB_CHARSET',  'utf8mb4');
define('DB_COLLATE',  '');

$table_prefix = 'wp_';

{% if wp_debug | default(false) %}
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
{% else %}
define('WP_DEBUG', false);
{% endif %}

if ( ! defined( 'ABSPATH' ) ) {
    define( 'ABSPATH', __DIR__ . '/' );
}
require_once ABSPATH . 'wp-settings.php';
```

### Template `templates/nginx-wordpress.conf.j2`

```jinja2
server {
    listen 80;
    server_name _;
    root {{ wp_dir }};
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php{{ php_version }}-fpm.sock;
    }

    location ~ /\.ht {
        deny all;
    }
}
```

### Ejecutar y verificar idempotencia

```bash
# Primera ejecución: despliega todo
ansible-playbook wordpress.yml

# Segunda ejecución: debe reportar changed=0
ansible-playbook wordpress.yml

# Antes de cualquier cambio: simula qué va a cambiar
ansible-playbook wordpress.yml --check --diff
```

WordPress estará disponible en `http://localhost:55001`.


## 🧹 Limpieza de las prácticas

Usamos comandos **ad-hoc** para limpiar rápido sin escribir un playbook.

```bash
# Limpiar módulo 3 — nginx en target1
ansible target1 -b -m service -a "name=nginx state=stopped enabled=no"
ansible target1 -b -m apt -a "name=nginx state=absent purge=yes"
ansible target1 -b -m user -a "name=nginx state=absent remove=yes"

# Limpiar módulo 4 — WordPress en target2
ansible target2 -b -m service -a "name=nginx state=stopped enabled=no"
ansible target2 -b -m shell -a "systemctl stop 'php*-fpm' && systemctl disable 'php*-fpm'" -e 'changed_when=false'
ansible target2 -b -m apt -a "name=nginx,php-fpm,php-mysql,php-curl,php-gd,php-mbstring,php-xml,php-zip state=absent purge=yes"
ansible target2 -b -m file -a "path=/var/www/wordpress state=absent"
ansible target2 -b -m user -a "name=wordpress state=absent remove=yes"

# Limpiar módulo 4 — MariaDB en target3
ansible target3 -b -m service -a "name=mariadb state=stopped enabled=no"
ansible target3 -b -m apt -a "name=mariadb-server,python3-pymysql state=absent purge=yes"
ansible target3 -b -m file -a "path=/var/lib/mysql state=absent"
```

> `-b` equivale a `become: yes`. Los ad-hocs son perfectos para operaciones puntuales: sin fichero, sin estado, sin overhead.


## 📝 Resumen del capítulo

* **`--check --diff`:** Simula cambios y muestra diffs antes de aplicar. Tu red de seguridad.
* **`ansible-doc`:** Documentación offline de cualquier módulo. Más rápido que Google.
* **`template` + Jinja2:** Configuraciones dinámicas con variables, condicionales y loops.
* **`lineinfile` / `blockinfile`:** Cirugía de archivos sin reemplazarlos enteros.
* **`command`/`shell` idempotentes:** `creates`, `changed_when`, `register` + `when`.
* **Módulos de aprovisionamiento:** `get_url` con checksum, `unarchive`, `git`.
* **`authorized_key`:** Gestión de acceso SSH de forma convergente.

**Próximo paso:** Variables, facts y plantillas más potentes para hacer tus playbooks verdaderamente dinámicos y reutilizables en cualquier entorno.
